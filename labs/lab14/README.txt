dataToTable module:
	makeList(string)
		Splits text from a CSV on commas
	
	makeTableBody(listOfLists)
		Returns a html table made of the list elements without the <table> tags
	
	makeTableHeader(listOfLists)
		Similar to makeTableBody, but uses <th> tags


htmlFuncts module:
	
